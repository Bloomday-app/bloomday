{
  "name": "bloomday",
  "version": "1.0.0",
  "description": "Bloomday — Célébrez chaque personne qui compte",
  "scripts": {
    "dev": "npx live-server --port=3000 --open=index.html",
    "build": "echo 'Static site — no build needed'",
    "netlify": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@supabase/supabase-js": "^2.105.3",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
